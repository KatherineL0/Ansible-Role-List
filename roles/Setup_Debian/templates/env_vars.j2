# Unused file and transfer task disabled.
# File to be moved to /etc/profile.d/.
# Sets environment variable for the current custom log directory location.

export CUSTOM_LOG_DIR={{ ansible_custom_log }}
export CUSTOM_AUTOLOG_DIR={{ ansible_custom_log }}/auto

